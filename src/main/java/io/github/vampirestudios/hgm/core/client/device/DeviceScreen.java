package io.github.vampirestudios.hgm.core.client.device;

import io.github.cottonmc.cotton.gui.GuiDescription;
import io.github.cottonmc.cotton.gui.client.ClientCottonScreen;

public class DeviceScreen extends ClientCottonScreen {
    //We'll probably want to completely redo the scaling in here, so subclass
    public DeviceScreen(GuiDescription description) {
        super(description);
    }

}
